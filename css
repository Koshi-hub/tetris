body {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  background-color: #222;
  margin: 0;
}

canvas {
  border: 1px solid #fff;
}

/*... 既存のスタイル... */

#score {
  position: absolute;
  top: 20px;
  left: 50%;
  transform: translateX(-50%);
}
